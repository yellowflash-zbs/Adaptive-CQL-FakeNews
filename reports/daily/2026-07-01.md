# 2026-07-01 日报

## 今日目标
- 规范项目代码管理流程，避免直接修改 `main` 分支。
- 推进 RAWFC / LIAR-RAW 下一阶段实验框架：bundle-level offline RL、证据包路由、多模式评估。
- 整理项目中过多的强化学习代码，只保留当前课题真正需要的方法。
- 建立后续周报素材记录方式。

## 今日完成工作
- 建立并使用实验分支 `codex/bundle-rl-experiments`，将不稳定实验代码与 `main` 分支隔离。
- 将 DeepSeek API Key 从代码中移除，改为通过环境变量 `DEEPSEEK_API_KEY` 读取，降低上传 GitHub 后泄漏密钥的风险。
- 新增证据包候选生成流程，支持 `claim_only`、`cosine_k3`、`cosine_k5`、`cql_mmr_k5_l0.5`、`cql_mmr_k3_l0.8`、`cql_top1`、`defense_support_refute` 等动作。
- 新增 bundle-level 离线 CQL / contextual bandit 控制器训练脚本，使强化学习从“句子级直接选句”扩展为“证据策略路由”。
- 重构 `evaluate.py`，支持 `random`、`cosine`、`current_cql`、`claim_only`、`defense_judge`、`bundle_rl` 等评估模式。
- 清理强化学习代码：删除体积过大的 `d4rl/rlkit`，只保留当前项目需要的 Adaptive CQL 和 bundle-level CQL 相关代码。
- 新增轻量 RL 模块：
  - `core/simple_rl.py`
  - `core/adaptive_cql.py`
- 更新 `weekly_report_notes.md`，记录本周开发过程，方便之后生成正式周报。
- 加固证据包候选生成脚本，改为流式读取大 JSON 特征文件，避免 RAWFC / LIAR-RAW 大文件一次性加载导致内存压力。
- 为候选生成与 bundle policy 训练增加调试后缀参数，防止小样本实验覆盖正式实验产物。
- 加固 `evaluate.py`，支持流式读取特征、指定 bundle policy checkpoint 后缀，以及不调用 DeepSeek 的证据选择 dry-run。
- 新增 `core/json_stream.py`，统一复用大 JSON 特征文件的流式读取逻辑。
- 修复日志模块在 Windows 终端中的 emoji 编码错误，保证评估脚本可以正常启动。

## 代码管理与备份
- 修改前已创建本地备份目录：
  - `backups/pre_bundle_scripts_20260701_124126`
  - `backups/pre_evaluate_modes_20260701_124627`
  - `backups/pre_cql_import_fix_20260701_124841`
  - `backups/pre_d4rl_delete_20260701_130142`
  - `backups/pre_daily_report_20260701_132000`
- 今日主要提交：
  - `2f89803 feat: add bundle-level RL scaffolding and secure API config`
  - `bd4717e feat: add evidence bundle generation and policy training`
  - `7cbacb0 feat: add multi-mode evaluation and weekly notes`
  - `f191ee3 refactor: replace vendored rlkit with minimal CQL components`

## 验证结果
- `python train.py --help` 正常。
- `python evaluate.py --help` 正常。
- Python 语法检查通过。
- RAWFC 旧 CQL checkpoint 可以通过新轻量模块正常加载，输出策略类型为 `TanhGaussianPolicy`。

## 当前结论
- 当前项目已经保留强化学习主线：句子级 Adaptive CQL + bundle-level 离线 CQL / contextual bandit。
- 冗余 RL 代码已大幅减少，后续实验会更容易维护。
- 证据包候选生成流程已更适合大规模数据集，可以先用 `--limit` 小样本验证，再切到全量运行。
- 多模式评估也可以先 dry-run 检查证据选择，再接入 DeepSeek 做正式指标。
- 下一步重点应放在生成证据包候选、训练 bundle policy、在 RAWFC 和 LIAR-RAW 验证集上做模式对比与消融实验。

## 明日建议
- 先在小样本上运行 `scripts/generate_evidence_bundles.py --skip-llm`，验证证据包候选文件格式。
- 再用少量样本接入 DeepSeek，检查 stance、reward、prediction 字段是否稳定。
- 最后训练 bundle policy，并在 `evaluate.py --mode bundle_rl` 下跑验证集结果。
