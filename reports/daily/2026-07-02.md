# 2026-07-02 日报

## 今日目标
- 继续推进 bundle-level offline RL 实验计划。
- 根据新的工作要求，将真正运行实验和得到结果的步骤交给研究者本人执行。
- 完善实验运行文档，让后续 RAWFC / LIAR-RAW 实验可以按规范复现。

## 今日完成工作
- 明确新的协作流程：Codex 负责改代码、静态检查、写实验步骤；正式跑实验、调用 DeepSeek、生成大文件和得到指标时，由研究者本人在电脑上执行。
- 加固 `scripts/extract_features.py`：
  - 移除硬编码 `CUDA_VISIBLE_DEVICES=3`。
  - 新增 `--device`，支持 `auto`、`cpu`、`cuda`。
  - 新增 `--cuda-visible-devices`，允许手动指定 GPU。
  - 新增 `--limit`、`--output-suffix`、`--overwrite`，方便先做小样本检查。
  - 移除终端 emoji 输出，降低 Windows 编码报错概率。
- 新增实验运行说明文档：`docs/experiment_steps_bundle_rl.md`。
- 补充 `requirements.txt` 中特征抽取需要的依赖：`transformers`、`nltk`、`pandas`、`rouge-score`。
- 为证据包生成和评估入口增加 `--feature-suffix`，使小样本调试特征文件可以继续进入后续流程。

## 当前结论
- 后续实验应先补齐 RAWFC / LIAR-RAW 的 val 特征文件，再生成证据包候选、训练 bundle policy、跑验证集对比。
- 正式运行前应先执行 `python -m pip install -r requirements.txt`，避免特征抽取时因缺少依赖中断。
- 小样本调试时，特征抽取产生的 `debug2` 文件需要在后续命令中通过 `--feature-suffix debug2` 指定。
- 当前阶段不建议直接跑 test；test 应保留到最终确认模型配置后再运行一次。

## 下一步建议
- 研究者先按照 `docs/experiment_steps_bundle_rl.md` 中的顺序，从小样本 `--limit 2` 开始检查特征抽取和证据包候选生成。
- 如果小样本无报错，再进入全量 val/train 流程。
